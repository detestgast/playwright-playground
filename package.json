{
  "scripts": {
    "allure": "allure generate allure-results --clean -o allure-report && allure open allure-report",
    "allure-clean": "rm -fR allure-results && rm -fR allure-report",
    "format": "prettier --write .",
    "pw": "playwright test",
    "pw-allure": "playwright test && allure generate allure-results --clean -o allure-report && allure open allure-report",
    "pwdebug": "playwright test --debug",
    "pwui": "playwright test --ui",
    "pwui-headed": "playwright test --ui --headed"
  },
  "name": "playwright-playground",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.0",
    "@trivago/prettier-plugin-sort-imports": "^6.0.2",
    "@types/node": "^26.0.0",
    "allure-playwright": "^3.10.0",
    "prettier": "^3.8.4"
  }
}
